<h1 align="center">Listing all articles</h1>
<div align="center">
  <%= will_paginate %>
</div>
<%= render 'article', obj: @articles %>
<div align="center">
  <%= will_paginate %>
</div>
	<% if logged_in? %>
      <div class="row">
        <div class= "col-xs-4 col-xs-offset-4">
          [<%= link_to 'Create a new Article', new_article_path, class: "ln_bottom" %>]
        </div>
      </div>
    <% end %>
